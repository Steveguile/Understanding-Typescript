let a: null;

a = null;
// a = 'Hi!'; // Error

// can be useful in union type to say it can also be null
let b: string | null;

// undefined is not the same as null. Means it can hold a specific type of undefined